dist/
node_modules/
src-backup/
test-results/
artifacts/

# 평면도 SVG 는 generate-floor-maps.mjs 가 생성한다. 포맷 대상 아님.
*.svg
src/features/radar/floor-maps.js

# 생성물
package-lock.json

# lms+ 가 실제로 서빙하는 스타일시트를 그대로 받아둔 픽스처.
# 포맷하면 원본과 달라져 호스트 CSS 충돌 재현이 흐려진다.
tests/fixtures/host-page.css
